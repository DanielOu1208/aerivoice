# beta.9 release readiness

Candidate: `v0.1.0-beta.9`, build `2026091701`, arm64, macOS 26+.
Baseline: published `v0.1.0-beta.8` (September 14, 2026).
Application source: `9dbf97a6145f66a8e35909f58a0b89f2f7709313`.
Draft PR: https://github.com/DanielOu1208/aerivoice/pull/4.
The release branch is pushed. [Draft release with verified assets](https://github.com/DanielOu1208/aerivoice/releases); no release tag has been created and nothing is published.
Subsequent documentation commits do not change the packaged application source.

## Scope and review

- Compose, Custom instructions, Polished prompt improvements, descriptive style chooser, and associated provider budgets, tests, evaluation tooling, and documentation.
- App, menu-bar, settings-sidebar, and README icon updates.
- Custom is a separate style based on Polished. The editor and instructions are active only in Custom; saved text survives switching styles. Settings are captured at recording start.
- Brief independent read-only review of the pre-existing beta.8 delta found no concrete release-blocking defects. The subsequent Custom selection change is reviewed and validated separately by the primary agent.
- Root `MEMORY.md` is unrelated and excluded from the release candidate.

## Validation

- Fresh isolated clone: **424 tests passed, 3 opt-in tests skipped, 0 failed**. Static analysis, unsigned Release app, and Release evaluation-harness builds passed.
- All **13** controlled local/Apple/model-provenance harness regressions passed.
- Full-history and working-tree redacted secret audits passed.
- Native settings acceptance passed in a temporary QA app with its own bundle ID/preferences: built-in styles hide the Custom editor, Custom reveals it, saved text survives switching, and Clear persists. Compose keeps its experimental warning. The sidebar microphone mark was visible.
- App-window captures could not establish the entire popover layout or actual menu-bar appearance. The compiled Release app icon was separately extracted and visually confirmed; README PNG exactly matches the 1024px app icon, and raster asset references/dimensions are valid.
- Unit tests verify saved custom instructions are excluded from built-in styles, empty Custom uses Polished, the mode persists, and recording-start settings remain fixed through later edits. Recording-icon rendering passed in both light and dark appearances.
- No new live/paid provider benchmark was run. Existing Compose semantic limitations remain documented in `docs/COMPOSE-VALIDATION.md`.
- Temporary QA process was stopped after testing. The user subsequently authorized updating the installed app; installation evidence is recorded below.

## Installer verification

- Application-source CI passed: [run 35283007582](https://github.com/DanielOu1208/aerivoice/actions/runs/35283007582), 7m47s. A later documentation-only commit records these results.
- Developer ID signing, hardened runtime, audio-input entitlement, and absence of debugger entitlement passed.
- Apple accepted app and DMG notarization (submission IDs `06ee5cca-0371-4232-8fbd-43c958838fd3` and `542e34f6-5d35-42b7-8a15-f5e62cb34c0b`). App and DMG stapled tickets and Gatekeeper verification passed.
- Read-only mounted DMG passed bundle ID, version/build, arm64 architecture, Applications shortcut, strict signature, notarization-ticket, Gatekeeper, and matching executable/symbol UUID checks.
- DMG SHA-256: `980ec991985372c4a576a3da1e51d737ed3c33efba36671c336c349f9bedb7fc`.
- Executable SHA-256: `49ebdb0f7500d6bf524fbfaff480b45ff6ff91bd3d80a69c55e1c98e56d43e59`.
- Executable and symbols UUID: `8D48D7E4-E48D-3247-B49D-A3C3E1345AB1`.
- Downloaded the draft GitHub asset and confirmed its checksum matches the local installer. This command-line download does not establish clean-user/quarantined-install acceptance.
- Source commit is bound by the clean build receipt and matching executable/symbol UUIDs. The bundle has no optional `AeriVoiceSourceRevision` field, despite the existing script passing that build setting; source identification uses the release receipt.
- Background signing returned `errSecInternalComponent`; the one-shot desktop-session job succeeded. It was removed after completion.
- Installer, checksum, symbols, and receipt are also in `~/Downloads/AeriVoice-v0.1.0-beta.9-review/`.

## Authorized local installation

- Updated `/Applications/AeriVoice.app` on the current MBP16, gracefully restarted, and verified one running executable at that path with the exact installer SHA-256 above.
- Native About confirms `0.1.0 (2026091701)`.
- Saved cleanup style, custom instructions, provider/model/reasoning choices, transcription provider, offline setting, shortcut, onboarding status, and vocabulary were preserved. Two encoded JSON settings changed only dictionary-key order; their values were equivalent.
- Existing Soniox and Cerebras release Keychain items remain present; no credentials were copied, reset, or logged. Live provider access was not exercised after installation.
- Previous app retained at `~/Downloads/AeriVoice-v0.1.0-beta.9-review/previous-installed/AeriVoice.app`.
- Installed Settings UI could not be reopened through the available accessibility surface. Custom behavior was verified in the isolated QA app before installation; installed build identity and saved values were verified separately.

## Remaining acceptance and publication gates

- [x] Native style switching, Custom editor visibility, saved-text restoration, sidebar icon, and compiled app icon. Actual menu-bar appearance remains unverified.
- [x] Final signed/notarized installer, stapling, Gatekeeper, checksum, and mounted bundle verification.
- [x] Fresh CI for the packaged application-source commit. Documentation-only follow-up CI is tracked on PR #4.
- [ ] Clean-user/quarantined installer onboarding and upgrade specifically from public beta.8. The authorized local update above was from a development build, not a public-beta.8 acceptance run.
- [ ] Real microphone dictation, cancellation, TextEdit and Discord/Electron insertion, launch at login, and default Gemini cleanup route.
- [ ] 96 → 48 → 96 kHz audio-route changes, shortcut hardware checks, and offline dictation with connectivity disabled.
- [ ] Approval before pushing a release tag or publishing, as specified in `docs/RELEASING.md`.
