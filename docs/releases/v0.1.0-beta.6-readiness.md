# beta.6 release readiness

Candidate: `v0.1.0-beta.6`, marketing version `0.1.0`, build `2026091202`.

The signed app was built from `9997f8d7e312e02ab3532cb3529c3ac12544fa52`.
Subsequent release-preparation changes update documentation only; app, project,
and test sources are unchanged from that tested commit.

## Scope

The release includes earlier Soniox connection setup, bounded Meta catch-up,
verified clipboard restoration, launch preparation, and optional local diagnostics.
Provider defaults and the sound-cue default remain unchanged.

New local evaluation harness code, transport adapters used only by that harness,
lifecycle observation hooks, fixture audio, run reports, credentials, and Keychain
administration helpers are excluded. Existing production diagnostics and their
regression tests remain part of the app source.

## Review

- Strict review covered capture and coordinator lifecycle, provider pacing,
  diagnostics storage and privacy, clipboard restoration, and release packaging.
- **Major, fixed:** disabled startup previously discarded only its
  recovery checkpoint, allowing expired completed history to survive later launches.
- **Minor, fixed:** updated privacy documentation to describe verified clipboard
  restoration and the current diagnostics retention limit.
- **Minor, fixed:** decomposed the 1,235-line coordinator test file into functional tests and
  scoped test support. Tests use controlled doubles and do not call live providers.
- No unresolved blocking finding remains from the source review. Device and
  destination-app behavior remain outside that review's evidence.

## Verification status

- Normal full suite: 319 passed, three opt-in live tests skipped, zero failures.
- Fresh-clone distribution full suite: 319 passed, three opt-in live tests skipped,
  zero failures.
- Focused diagnostics retention tests: 24 passed, including three new regressions.
- Normal and distribution Release static analysis and unsigned arm64 builds passed.
- Full-history and local-file secret audits and whitespace checks passed.
- [GitHub CI for the app-source commit](https://github.com/DanielOu1208/aerivoice/actions/runs/34738015075)
  passed tests, static analysis, and the Release build.
- Developer ID signed archive and DMG passed hardened-runtime, entitlement,
  architecture, version, signature, stapling, and Gatekeeper checks.
- App notarization accepted: `4032b41f-b287-453e-81c2-f11f66288853`.
- DMG notarization accepted: `0109f15f-1e59-4e53-ac92-6adc6e002716`.
- The mounted installer app's executable matches the signed archive byte-for-byte.
- Executable SHA-256: `5aca4aef4d7ebb1d6b30c36ce2b363304f46a9cc5fff32dc76c7dc6b9602d941`.
- App and symbol UUID: `0D6472CF-CBDD-3A60-A69C-9BB318E1C888` (arm64).
- DMG SHA-256: `268a1fecec6b1a6c1c208702c188d59e092ea2920205a9cc7ea6952eeaf063dd`.
- Artifacts: `dist/v0.1.0-beta.6/`, containing the signed DMG, checksum, symbol
  archive, and local build receipt. New benchmark artifacts are excluded.

## Manual checks remaining

Fresh-user quarantined installation/onboarding, beta.5 credential/settings upgrade,
microphone-to-provider-to-paste dictation, launch at login, default cleanup route,
and 96/48 kHz audio-route transitions have not been repeated for this artifact.
TextEdit and Discord/Electron insertion also require current-artifact acceptance.
Automated cancellation and clipboard tests do not establish full real-app coverage.

The GitHub release is prepared as a draft. Publishing and pushing the release tag
remain separate steps under `docs/RELEASING.md`.
