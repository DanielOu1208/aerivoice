# Artwork provenance

The AeriVoice app icon and social preview were created specifically for this
repository in September 2026 under Daniel Ou's direction. The mark is an
original monochrome line drawing: a faceted octagonal microphone head with a
crossing web of facets, resting in a rounded yoke on a tripod foot. It is
drawn on a 40 × 40 grid with uniform 1.3-unit strokes and round caps, and is
reproduced in this repository as vector art in `aerivoiceweb/brand/`, from
which every raster asset is generated by `aerivoiceweb/brand/build-assets.sh`.

The mark replaces the earlier treatment (a near-black tile carrying the Aeri
script wordmark with a microphone as the dot above the `i`). That wordmark
remains part of the wider Aeri brand family and is unchanged elsewhere; it no
longer appears in AeriVoice's own icon.

The app icon has an opaque white background extending to every canvas edge,
with the mark in near-black ink. The native `AeriVoice/AppIcon.icon` package
keeps the white fill in its artwork for both light and dark icon appearances;
macOS supplies the outer icon shape. Xcode prefers this package over the
legacy `AppIcon.appiconset` raster fallback. The selected design is round 7, S10 (Yoke & Tripod) in
`aerivoiceweb/logo-candidates.html`. Small app icons and favicons use a solid
head without internal facets. The menu bar likewise uses this solid-head
variant as a template image so it follows the menu bar's light and
dark appearance. While dictation is active, the app renders native
`NSColor.systemOrange` into a non-template image so the status bar preserves
the orange. Idle uses a template image that follows the menu bar appearance.

The final artwork is original project artwork, is not an Apple system icon or
sound, and is distributed with this repository under the MIT License.

The project does not distribute the macOS native alert sounds used at runtime.
AeriVoice requests `Blow`, `Bottle`, and `Submarine` through `NSSound` on the
user's Mac.

## README demo

`images/aerivoice-demo.gif` is derived from Daniel Ou's supplied
`aerivoice qwen demo.mp4` recording from September 3, 2026. The full recording
retains its original timing, with no audio, at 800 × 600 and 20 frames per second.
It loops continuously. Palette quantization uses 96 colors and Bayer dithering (scale 5);
the GIF is approximately 2.7 MB. The source recording remains outside the repo.

## Provider logos

Provider logos identify supported services and remain trademarks of their
respective owners. They are not original AeriVoice artwork or an endorsement.

| Asset | Source |
| --- | --- |
| `images/providers/soniox.png` | [Soniox's official favicon](https://soniox.com/icons/favicon-96x96.png), retrieved September 7, 2026; unchanged |
| `images/providers/meta.svg` | Lobe Icons `meta-color.svg` |
| `images/providers/spacex.png` | [SpaceX's official favicon](https://www.spacex.com/assets/favicon-spacex.ico), retrieved September 19, 2026 |
| `images/providers/openrouter.svg` | [OpenRouter's official light-background mark](https://github.com/OpenRouterTeam/sign-in-with-openrouter/blob/main/public/openrouter-logo-light.svg), retrieved September 20, 2026 |
| `images/providers/cerebras.svg` | `Cerebras C logo.svg` from the [official press kit](https://www.cerebras.ai/company/press-kit), retrieved September 20, 2026 |
| `images/providers/groq.svg` | Lobe Icons `groq.svg` |

Meta and Groq come from [Lobe Icons at commit a94750e](https://github.com/lobehub/lobe-icons/tree/a94750e3f5f8fc33757b839d85030e742284e43a/packages/static-svg/icons).
They retain the source paths and brand colors, with a white rounded backing and
padding for contrast in light and dark GitHub themes. Unspecified/current colors
are fixed to dark ink for standalone image rendering. Lobe Icons is MIT licensed;
its copyright and license are included in
[`images/providers/LICENSE-LobeHub.txt`](images/providers/LICENSE-LobeHub.txt).

OpenRouter and Cerebras preserve their official SVG paths and colors with
proportional scaling and white rounded backings. OpenRouter uses Grape
(`#7624F4`) as specified for light backgrounds in its [brand guidance](https://openrouter.ai/brand).
Cerebras retains the multi-ring C supplied in its
[press-kit archive](https://cdn.sanity.io/files/e4qjo92p/production/047798c0db861ade2a18320e534c00f05883a180.zip).
The corresponding native PNGs are rendered from these documentation SVGs.

SpaceX's complete 48 × 48 favicon is proportionally scaled into a 96 × 96 black
tile with 9 pixels of clear space on each side, protecting the mark from the
native icon's rounded clipping. The app and README use the same PNG. The SpaceX
mark identifies the Grok (xAI) provider at Daniel's direction.

SpaceX, Soniox, OpenRouter, and Cerebras assets are third-party brand assets and
are not covered by AeriVoice's or LobeHub's MIT license. No permissive license is
asserted for these official marks. App asset notices are recorded in
[`../AeriVoice/ProviderIcons-LICENSE.txt`](../AeriVoice/ProviderIcons-LICENSE.txt).
